version: '3.8'

services:
  app:
    build: .
    environment:
      - ACCOUNTS
      - BETTERSTACK_TOKEN
      - DISCORD_WEBHOOK_URL
      - INTERVAL
      - REQUESTER_EMAIL
      - RPC
    restart: always
